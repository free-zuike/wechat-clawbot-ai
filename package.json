{
  "name": "wechat-clawbot-ai",
  "version": "1.0.0",
  "private": true,
  "description": "ClawBot AI — 基于 Cloudflare Worker + Worker AI 的微信个人号机器人 (iLink 协议)",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "start": "wrangler dev --test-scheduled",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "base64-js": "^1.5.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.25.0"
  }
}
